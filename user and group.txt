/etc/passwd 			#---Информацию о каждом пользователе сервера можно посмотреть в файле 
sed 's/:.*//' /etc/passwd	#---получить список пользователей Linux, состоящий только из имен

[ilya@client-rhcent ~]$ cat /etc/passwd | grep httpd
apache:x:48:48:Apache:/usr/share/httpd:/sbin/nologin

who 	#---список активных пользователей, включая их имена, терминалы, с которых они вошли в систему, дату и время входа, а также IP-адрес, если доступен.

users	#---получить список имен пользователей, которые в данный момент вошли в систему с помощью командной строки (терминала) или через удаленное подключение, введите в консоль.

w	#--демонстрирует подробный список активных пользователей, включая их текущие действия;

useradd -D	#---




