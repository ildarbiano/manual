1\ change linux hostname
sudo hostnamectl set-hostname <newhostname>
sudo reboot

apt remove ansible
sudo apt-get remove –purge ansible

sudo apt-get install tree
tree
sudo apt-get install lsof
sudo lsof /var/lib/dpkg/lock-frontend


less

